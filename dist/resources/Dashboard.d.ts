import { CfnResource } from "aws-cdk-lib";
import { DashboardProps } from "../types/Dashboard";
export declare class Dashboard extends CfnResource {
    constructor(id: string, props: DashboardProps);
}
